enum TextFieldSuffixIconType { cancel, showObscureText, hideObscureText }

enum AppLoaderTheme { light, dark }

enum SharedPreferenceStore {
  IS_USER_LOGGED_IN,
  IS_LIGHT_THEME,
  SERVER_TOKEN,
  LANGUAGE_CODE,
  USER_DETAILS
}

enum LoginTextFieldEnum { username, password }